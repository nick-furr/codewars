function min(list) {
  // Using Math.min with the spread operator to find the minimum value in the array.
  return Math.min(...list);
}
​
function max(list) {
  // Using Math.max with the spread operator to find the maximum value in the array.
  return Math.max(...list);
}